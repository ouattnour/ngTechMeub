import { DefaulImagePipe } from './defaul-image.pipe';

describe('DefaulImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DefaulImagePipe();
    expect(pipe).toBeTruthy();
  });
});
